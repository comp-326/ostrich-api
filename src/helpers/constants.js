const ROLES = {
	admin: 'admin',
	creator: 'creator',
	counselor: 'counselor',
}

const APPT_STATE = {
	pending: 'pending',
	canceled: 'canceled',
	done: 'done',
	confirmed: 'confirmed',
}

module.exports = { ROLES, APPT_STATE }
